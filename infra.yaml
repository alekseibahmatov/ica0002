---
  - name: Init
    hosts: all
    become: yes
    roles:
      - init

  - name: DNS server
    hosts: dns_server
    become: yes
    gather_facts: no
    roles:
      - bind

  - name: Database server
    hosts: db_servers
    become: yes
    roles:
      - mysql

  - name: Web server
    hosts: web_servers
    become: yes
    gather_facts: no
    roles:
      - agama
      - uwsgi
      - nginx
